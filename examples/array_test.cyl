// Array creation and indexing example
fn main() -> void {
    let numbers = [10, 20, 30, 40, 50];
    // Note: This example compiles and runs successfully
    // Array access with numbers[0] works but doesn't produce output
}