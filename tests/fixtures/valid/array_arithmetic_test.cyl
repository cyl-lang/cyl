// Test array indexing in arithmetic expressions
fn main() -> void {
    let nums = [5, 10, 15];
    
    // Use array indexing in arithmetic
    let sum = nums[0] + nums[1] + nums[2];  // Should be 5 + 10 + 15 = 30
    let product = nums[0] * nums[1];        // Should be 5 * 10 = 50
    
    print_int(sum);
    print_int(product);
}
